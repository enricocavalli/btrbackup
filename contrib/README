
Questa directory contine lo script da copiare in init.d per la partenza
automatica dello scheduler

# cp scheduler.init /etc/init.d/scheduler
# update-rc.d -f scheduler defualts

E' presente inoltre uno script di esempio per configurare un client generico

